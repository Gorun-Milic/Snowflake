export default class Day {

    constructor(weekday, date, city, weather) {
        this.weekday = weekday;
        this.date = date;
        this.city = city;
        this.weather = weather;
    }

}